name: authentication_kit
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  http:
  intl:
  logger:
  url_launcher:
  flutter_native_splash:
  smooth_page_indicator:
  connectivity_plus:
  lottie:

  iconsax:
  cupertino_icons:

  get:
  get_storage:

  readmore:
  carousel_slider:
  flutter_rating_bar:
  capped_progress_indicator:

  firebase_core:
  firebase_auth: ^6.0.0
  cloud_firestore:
  firebase_storage:
  google_sign_in: ^7.1.1
  shimmer: ^3.0.0
  google_fonts: ^6.3.0
  font_awesome_flutter: ^10.9.1
  icons_plus: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

# assets
  assets:
    - assets/images/
    - assets/logos/app_logo.jpg
    - assets/logos/